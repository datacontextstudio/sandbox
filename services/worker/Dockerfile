# TODO: replace this stub with the real ingestion worker Dockerfile
# Worker responsibilities: consume from Redis queue, parse (Docling),
# chunk, embed via ollama, and index into qdrant.
FROM python:3.12-slim
WORKDIR /app
CMD ["python", "-c", "import time; print('worker stub running'); time.sleep(3600)"]
